import 'package:flutter/material.dart';

class WaterToday extends StatelessWidget {
  const WaterToday({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      height: 200,
    );
  }
}
