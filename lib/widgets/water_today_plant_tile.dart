import 'package:flutter/material.dart';

class WaterTodayPlantTile extends StatelessWidget {
  const WaterTodayPlantTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
