import 'package:flutter/material.dart';

class PlantListTile extends StatelessWidget {
  const PlantListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
